import React from "react";

const Add = () => {
  return (
    <form>
      <label htmlFor="promotion">Add Promotion</label>
      <input type="file" name="promotion" id="promotion" />
    </form>
  );
};

export default Add;
